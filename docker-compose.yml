services:
  database:
    image: postgres:15-alpine
    container_name: travel_db
    environment:
      POSTGRES_USER: travel_user
      POSTGRES_PASSWORD: travel_password
      POSTGRES_DB: travel_db
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./database/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
      - ./database/migrations/002_add_rbac.sql:/docker-entrypoint-initdb.d/002_add_rbac.sql
    networks:
      - travel-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U travel_user -d travel_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    container_name: travel_backend
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DB_HOST: database
      DB_USER: travel_user
      DB_PASSWORD: travel_password
      DB_NAME: travel_db
      # URL publique utilisée dans les liens de vérification et de réinitialisation.
      PUBLIC_APP_URL: ${PUBLIC_APP_URL:-https://travelms.example.com}
      # Secret long et aléatoire obligatoire : ne jamais fournir de valeur par défaut.
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:?Définissez JWT_ACCESS_SECRET avant le déploiement}
    depends_on:
      database:
        condition: service_healthy
    networks:
      - travel-net
    volumes:
      - ./uploads:/app/public/uploads
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: travel_frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    networks:
      - travel-net
    restart: unless-stopped

volumes:
  pgdata:

networks:
  travel-net:
    driver: bridge
