server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Point d'entrée explicite de la SPA. Ne pas rediriger /index.html vers /
    # ici : `try_files $uri $uri/` trouve le répertoire racine pour /, puis
    # Nginx charge index.html. Une redirection de cette ressource vers / crée
    # alors une boucle infinie (notamment visible sur un accès par IP).
    location = / { try_files /index.html =404; }
    location = /index.html { try_files /index.html =404; }

    # Compatibilité avec les URLs présentes dans les e-mails générés par le backend
    # et les favoris de l'ancienne application.
    location = /login.html { return 301 /login; }
    location = /register.html { return 301 /register; }
    location = /verify-email.html { return 302 /verify-email$is_args$args; }
    location = /forgot-password.html { return 301 /forgot-password; }
    location = /reset-password.html { return 302 /reset-password$is_args$args; }
    location = /change-password.html { return 301 /change-password; }
    location = /dashboard.html { return 301 /dashboard; }
    location = /profile.html { return 301 /profile; }
    location = /my-bookings.html { return 301 /bookings; }
    location = /destination.html { if ($arg_id != "") { return 302 /destinations/$arg_id; } return 301 /destinations; }
    location = /destination-detail.html { if ($arg_id != "") { return 302 /destinations/$arg_id; } return 301 /destinations; }
    location = /catalog.html { if ($arg_type != "") { return 302 /catalog/$arg_type; } return 301 /catalog/circuits; }
    location = /catalog-detail.html { if ($arg_type != "") { return 302 /catalog/$arg_type/$arg_id; } return 301 /catalog/circuits; }
    location = /booking.html {
        if ($arg_tour_id != "") { return 302 /booking/$arg_tour_id$is_args$args; }
        if ($arg_circuit_id != "") { return 302 /booking/$arg_circuit_id$is_args$args; }
        if ($arg_destination_id != "") { return 302 /bookings/new?destinationId=$arg_destination_id; }
        return 301 /catalog/circuits;
    }
    location = /admin.html { return 301 /admin; }
    location = /admin-bookings.html { return 301 /admin/bookings; }
    location = /admin-destinations.html { return 301 /admin/destinations; }
    location = /admin-catalog.html { if ($arg_entity != "") { return 302 /admin/catalog/$arg_entity; } return 301 /admin/catalog/circuits; }

    # L'API et les médias restent servis par Node : une seule origine évite CORS.
    location /api/ {
        proxy_pass http://backend:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        proxy_pass http://backend:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Les deep links React (/destinations/:id, /bookings, etc.) retournent index.html.
    # $uri/ est volontairement absent : il ne doit jamais sélectionner / pour
    # retomber sur la page index via le mécanisme d'index Nginx.
    location / {
        try_files $uri /index.html;
    }
}
