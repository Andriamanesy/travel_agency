import { AlertTriangle } from 'lucide-react'

export function ConfirmDialog({ open, title, description, confirmLabel = 'Confirmer', danger = false, pending = false, onCancel, onConfirm }: { open: boolean; title: string; description: string; confirmLabel?: string; danger?: boolean; pending?: boolean; onCancel: () => void; onConfirm: () => void }) {
  if (!open) return null
  return <div role="presentation" className="fixed inset-0 z-[100] grid place-items-center p-4" onMouseDown={(event) => { if (event.target === event.currentTarget && !pending) onCancel() }}><div className="absolute inset-0 bg-slate-950/55 backdrop-blur-sm" /><section role="dialog" aria-modal="true" aria-labelledby="confirm-title" className="relative w-full max-w-md rounded-3xl bg-white p-7 shadow-2xl"><span className={`grid h-12 w-12 place-items-center rounded-2xl ${danger ? 'bg-red-50 text-red-700' : 'bg-amber-50 text-amber-700'}`}><AlertTriangle size={23} /></span><h2 id="confirm-title" className="mt-5 text-2xl font-black text-slate-950">{title}</h2><p className="mt-2 leading-6 text-slate-600">{description}</p><div className="mt-7 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><button type="button" onClick={onCancel} disabled={pending} className="rounded-xl px-5 py-3 text-sm font-bold text-slate-600 hover:bg-slate-100 disabled:opacity-60">Annuler</button><button type="button" onClick={onConfirm} disabled={pending} className={`rounded-xl px-5 py-3 text-sm font-bold text-white disabled:opacity-60 ${danger ? 'bg-red-700 hover:bg-red-800' : 'bg-emerald-700 hover:bg-emerald-800'}`}>{pending ? 'Confirmation…' : confirmLabel}</button></div></section></div>
}
