import { zodResolver } from '@hookform/resolvers/zod'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { useState } from 'react'
import { useForm } from 'react-hook-form'
import { z } from 'zod'
import { ApiError } from '@/lib/api-client'
import { ConfirmDialog } from '@/components/feedback/ConfirmDialog'
import { adminRbacService, type AdminManagedUser } from '../services/admin-rbac.service'

const schema = z.object({ name: z.string().trim().min(1, 'Le nom est requis.').max(100), email: z.email('Adresse e-mail invalide.'), password: z.string().min(12, '12 caractères minimum.').max(200).optional().or(z.literal('')), role_id: z.number().int().positive('Sélectionnez un rôle.') })
type Values = z.infer<typeof schema>
const usersKey = ['admin', 'dynamic-users'] as const
export function AdminUsersPage() {
  const client = useQueryClient(); const [query, setQuery] = useState(''); const [role, setRole] = useState(''); const [status, setStatus] = useState(''); const [editing, setEditing] = useState<AdminManagedUser | null>(null); const [confirmation, setConfirmation] = useState<{ user: AdminManagedUser; action: 'delete' | 'status' } | null>(null)
  const parameters = new URLSearchParams(); if (query) parameters.set('q', query); if (role) parameters.set('role_id', role); if (status) parameters.set('status', status); const suffix = parameters.size ? `?${parameters}` : ''
  const users = useQuery({ queryKey: [...usersKey, suffix], queryFn: () => adminRbacService.users(suffix) }); const roles = useQuery({ queryKey: ['admin', 'rbac', 'roles'], queryFn: adminRbacService.roles })
  const refresh = () => client.invalidateQueries({ queryKey: usersKey }); const save = useMutation({ mutationFn: ({ id, values }: { id: string | null; values: Values }) => id ? adminRbacService.updateUser(id, values) : adminRbacService.createUser({ ...values, password: values.password || undefined }), onSuccess: () => { refresh(); setEditing(null) } }); const action = useMutation({ mutationFn: ({ user, kind }: { user: AdminManagedUser; kind: 'delete' | 'status' }) => kind === 'delete' ? adminRbacService.deleteUser(user.id) : adminRbacService.setStatus(user.id, !user.is_active), onSuccess: () => { refresh(); setConfirmation(null) } })
  const error = [users.error, roles.error, save.error, action.error].find((item) => item instanceof ApiError)
  return <section><p className="text-sm font-semibold uppercase tracking-[.3em] text-emerald-600">Administration & sécurité</p><div className="mt-2 flex flex-wrap items-center justify-between gap-4"><div><h1 className="text-3xl font-black">Utilisateurs</h1><p className="mt-2 text-slate-600">Créez, modifiez, activez et gérez les comptes.</p></div><button className="action" onClick={() => setEditing({ id: '', name: '', email: '', avatar_url: null, is_verified: true, is_active: true, role_id: 0, created_at: '' })}>Créer un utilisateur</button></div><div className="mt-7 flex flex-wrap gap-3 rounded-2xl bg-white p-4 shadow-sm"><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Rechercher nom ou e-mail" className="field max-w-sm" /><select value={role} onChange={(event) => setRole(event.target.value)} className="field w-auto"><option value="">Tous les rôles</option>{roles.data?.roles.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><select value={status} onChange={(event) => setStatus(event.target.value)} className="field w-auto"><option value="">Tous les statuts</option><option value="active">Actif</option><option value="banned">Banni</option></select></div>{error && <p role="alert" className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error.message}</p>}<div className="mt-6 overflow-x-auto rounded-3xl bg-white shadow-sm"><table className="w-full min-w-[760px] text-left"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-4">Utilisateur</th><th className="p-4">Rôle</th><th className="p-4">Statut</th><th className="p-4">Créé le</th><th className="p-4">Actions</th></tr></thead><tbody>{users.isPending ? <tr><td colSpan={5} className="p-6">Chargement…</td></tr> : users.data?.users.map((user) => <tr key={user.id} className="border-t border-slate-100"><td className="p-4"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-emerald-100 font-bold text-emerald-800">{user.name[0]?.toUpperCase()}</span><span><b className="block">{user.name}</b><small className="text-slate-500">{user.email}</small></span></div></td><td className="p-4"><span className="rounded-full bg-slate-100 px-3 py-1 text-sm">{user.role_name || user.role_code || '—'}</span></td><td className="p-4"><span className={`rounded-full px-3 py-1 text-sm font-semibold ${user.is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-red-50 text-red-700'}`}>{user.is_active ? 'Actif' : 'Banni'}</span></td><td className="p-4 text-sm text-slate-500">{user.created_at ? new Date(user.created_at).toLocaleDateString('fr-FR') : '—'}</td><td className="p-4"><div className="flex gap-3 text-sm font-semibold"><button className="text-emerald-700" onClick={() => setEditing(user)}>Modifier</button><button className="text-amber-700" onClick={() => setConfirmation({ user, action: 'status' })}>{user.is_active ? 'Bannir' : 'Activer'}</button><button className="text-red-700" onClick={() => setConfirmation({ user, action: 'delete' })}>Supprimer</button></div></td></tr>)}</tbody></table></div>{users.data && <p className="mt-3 text-sm text-slate-500">{users.data.pagination.total} utilisateur(s)</p>}{editing && <UserModal user={editing} roles={roles.data?.roles ?? []} pending={save.isPending} onClose={() => setEditing(null)} onSave={(values) => save.mutate({ id: editing.id || null, values })} />}<ConfirmDialog open={Boolean(confirmation)} title={confirmation?.action === 'delete' ? 'Supprimer ce compte ?' : `${confirmation?.user.is_active ? 'Bannir' : 'Activer'} ce compte ?`} description={confirmation?.action === 'delete' ? 'Cette action supprime définitivement le compte.' : 'Le changement est appliqué immédiatement.'} danger={confirmation?.action === 'delete'} pending={action.isPending} confirmLabel={confirmation?.action === 'delete' ? 'Supprimer' : 'Confirmer'} onCancel={() => setConfirmation(null)} onConfirm={() => confirmation && action.mutate({ user: confirmation.user, kind: confirmation.action })} /></section>
}

function UserModal({ user, roles, pending, onClose, onSave }: { user: AdminManagedUser; roles: { id: number; name: string }[]; pending: boolean; onClose: () => void; onSave: (values: Values) => void }) {
  const { register, handleSubmit, formState: { errors } } = useForm<Values>({ resolver: zodResolver(schema), defaultValues: { name: user.name, email: user.email, password: '', role_id: user.role_id } })
  return <div className="fixed inset-0 z-[90] grid place-items-center overflow-y-auto bg-slate-950/55 p-4"><form onSubmit={handleSubmit(onSave)} className="w-full max-w-lg rounded-3xl bg-white p-7 shadow-2xl"><h2 className="text-2xl font-black">{user.id ? 'Modifier l’utilisateur' : 'Créer un utilisateur'}</h2><p className="mt-1 text-sm text-slate-500">{user.id ? 'Laissez le mot de passe vide pour le conserver.' : 'Le mot de passe doit contenir 12 caractères minimum.'}</p><div className="mt-6 space-y-4"><label className="block text-sm font-semibold">Nom<input {...register('name')} className="field" /></label>{errors.name && <p className="text-sm text-red-700">{errors.name.message}</p>}<label className="block text-sm font-semibold">E-mail<input {...register('email')} type="email" className="field" /></label>{errors.email && <p className="text-sm text-red-700">{errors.email.message}</p>}<label className="block text-sm font-semibold">Mot de passe<input {...register('password')} type="password" autoComplete="new-password" className="field" /></label>{errors.password && <p className="text-sm text-red-700">{errors.password.message}</p>}<label className="block text-sm font-semibold">Rôle<select {...register('role_id', { setValueAs: (value) => Number(value) })} className="field"><option value="">Sélectionner</option>{roles.map((role) => <option key={role.id} value={role.id}>{role.name}</option>)}</select></label>{errors.role_id && <p className="text-sm text-red-700">{errors.role_id.message}</p>}</div><div className="mt-7 flex justify-end gap-3"><button type="button" onClick={onClose} className="rounded-xl px-4 py-3 font-semibold">Annuler</button><button disabled={pending} className="action">{pending ? 'Enregistrement…' : 'Enregistrer'}</button></div></form></div>
}
