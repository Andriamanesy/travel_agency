import { apiRequest } from '../api.js';
const params = new URLSearchParams(location.search), type = params.get('type'), id = params.get('id'); const root = document.getElementById('detail');
if (!['circuits', 'hotels', 'guides'].includes(type) || !id) root.textContent = 'Offre introuvable.'; else load();
async function load() { try { const response = await apiRequest(`/${type}/${encodeURIComponent(id)}`); const item = response[type.slice(0,-1)]; const image = item.cover_image || item.avatar_url || ''; const price = item.price ?? item.price_per_night; root.innerHTML = `<a href="/catalog.html?type=${type}" class="font-semibold text-blue-600">← Retour au catalogue</a>${image ? `<img src="${image}" class="mt-7 h-96 w-full rounded-3xl object-cover">` : ''}<section class="mt-8 rounded-3xl bg-white p-8 shadow-sm"><h1 class="text-4xl font-black"></h1><p class="mt-5 whitespace-pre-line leading-8 text-slate-600"></p>${price !== undefined ? `<p class="mt-6 text-2xl font-black text-blue-700">${Number(price).toFixed(2)} €${type === 'hotels' ? ' / nuit' : ''}</p>` : ''}${type === 'circuits' ? `<a href="/booking.html?circuit_id=${item.id}" class="mt-7 inline-block rounded-xl bg-blue-600 px-5 py-3 font-semibold text-white">Réserver ce circuit</a>` : ''}</section>`; root.querySelector('h1').textContent = item.title || item.name; root.querySelector('p').textContent = item.description || item.bio || item.address || ''; } catch (error) { root.textContent = error.message; } }
